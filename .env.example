# ----- Appwrite Cloud (Vercel / Production) -----
# https://cloud.appwrite.io — Proje > Settings > API
NEXT_PUBLIC_APPWRITE_ENDPOINT="https://cloud.appwrite.io/v1"
NEXT_PUBLIC_APPWRITE_PROJECT_ID="your-project-id"
# Sunucu tarafı (API route, Server Components): Proje > API Keys > Create API Key (Admin)
APPWRITE_API_KEY="your-api-key-secret"

# ----- Eski (Prisma/NextAuth — migrasyon sonrası kaldırılacak) -----
# DATABASE_URL="postgresql://user:password@localhost:5432/sosyalcan?schema=public"
# NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"
# NEXTAUTH_URL="http://localhost:3000"
